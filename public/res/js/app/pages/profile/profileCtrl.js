'use strict';

define(['appModule'], function (app) {

        app.controller('profileController', ['$scope', '$q', '$rootScope', '$routeParams', '$location', '$http',
            function ($scope, $q, $rootScope, $routeParams, $location, $http) {
                    
            }
        ]);
});
